export interface Icon {
  keywords: string[];
  imagePath: string;
  isRemoteImage: boolean;
}

export interface IconttvIcon {
  keywords: string[];
  tags: string[];
  useOrigin: boolean;
  uri: string;
  originUri: string;
  nameHash: string;
}
